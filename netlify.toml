[build]
  command   = "bash scripts/netlify-flutter-build.sh"
  publish   = "build/web"

[build.environment]
  FLUTTER_DIR = ".flutter"

[build.cache]
  paths = [
    ".flutter",
    "~/.pub-cache"
  ]
